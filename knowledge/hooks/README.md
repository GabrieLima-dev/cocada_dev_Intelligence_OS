# Biblioteca de estruturas de hook

Hooks são estruturas testáveis, não frases para copiar. Cada uso registra estrutura, versão, mecanismo, primeiro frame, payoff e resultado observado no próprio conteúdo. Nunca atribua resultado global a um único caso.
