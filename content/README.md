# Ciclo de conteúdo

`inbox → ideas → selected → scripts → production → published → archive`. Mover/copiar artefatos sem trocar o `content_id`; preserve histórico de versões relevante e relações no grafo.
